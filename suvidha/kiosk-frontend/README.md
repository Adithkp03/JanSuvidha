# This README explains how to run the JanSuvidha kiosk frontend (Vite + React) locally.

## Run locally (dev)
```bash
npm install
npm run dev
```

## Notes
- In Docker/compose, the kiosk UI is served under `http://localhost/kiosk/`.

