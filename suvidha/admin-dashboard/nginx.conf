# This nginx config serves the admin SPA build and supports client-side routing (Phase 0).
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}

