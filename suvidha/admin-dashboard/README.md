# This README explains how to run the JanSuvidha admin dashboard (Vite + React) locally.

## Run locally (dev)
```bash
npm install
npm run dev
```

## Notes
- In Docker/compose, the admin UI is served under `http://localhost/admin/`.

