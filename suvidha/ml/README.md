# This folder is reserved for ML training scripts and model artifacts for JanSuvidha (Phase 1+).

- Put notebooks / training code under `ml/training/`
- Store versioned model artifacts under `ml/artifacts/` (avoid committing large binaries unless needed)

