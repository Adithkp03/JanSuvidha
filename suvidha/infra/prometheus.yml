# This Prometheus config is a minimal stub to scrape the API gateway health endpoint for Phase 0 demos.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "api-gateway"
    metrics_path: /metrics
    static_configs:
      - targets: ["api-gateway:3000"]

