# This README explains how to run the JanSuvidha Request service stub locally.

## Endpoints (Phase 0)
- `GET /health`
- `GET /requests`
- `POST /requests`
- `GET /requests/:id`

## Run locally (without compose)
```bash
cp .env.example .env
npm install
npm run start
```

