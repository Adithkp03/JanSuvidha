# This README explains how to run the JanSuvidha payment service sandbox stub locally.

## Endpoints (Phase 0)
- `GET /health`
- `POST /payments`

## Run locally (without compose)
```bash
cp .env.example .env
npm install
npm run start
```

